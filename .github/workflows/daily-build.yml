name: Build Docker Image

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
  strategy:
    matrix:
      guide: [ guide-getting-started, guide-cdi-intro ]
  steps:
      - name: Run tests
      run: echo test ${{ matrix.guide }}
